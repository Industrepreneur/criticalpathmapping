'use strict';

angular.module('mapping.accounts', [
  'mapping.accounts.filters',
  'mapping.accounts.services'
]);

angular.module('mapping.accounts.filters', []);

angular.module('mapping.accounts.services', []);
