'use strict';

angular.module('mapping.utils', [
  'mapping.utils.directives',
  'mapping.utils.services'
]);

angular.module('mapping.utils.directives', []);

angular.module('mapping.utils.services', []);
