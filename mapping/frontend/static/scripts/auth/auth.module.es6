'use strict';

angular.module('mapping.auth', [
  'mapping.auth.controllers',
  'mapping.auth.services'
]);

angular.module('mapping.auth.controllers', []);

angular.module('mapping.auth.services', ['ngCookies']);
