'use strict';

angular.module('mapping.parts', [
  'mapping.parts.controllers',
  'mapping.parts.directives',
  'mapping.parts.routes',
  'mapping.parts.services'
]);

angular.module('mapping.parts.controllers', []);

angular.module('mapping.parts.directives', []);

angular.module('mapping.parts.services', []);

angular.module('mapping.parts.routes', []);
